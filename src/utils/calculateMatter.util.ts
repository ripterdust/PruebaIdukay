import { noCompile } from '../misc/responses.response'
import { CalculateMatter } from '../types/calculateMatter.type'

export const calculateMatter: CalculateMatter = (firstRocket) => {
    return noCompile
}
